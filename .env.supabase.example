# ==============================================================================
# CURSIVE - SUPABASE CONFIGURATION TEMPLATE
# ==============================================================================
# Copy this file to .env.supabase and fill in your actual values
# Get these values from: https://supabase.com/dashboard > Your Project > Settings > API
# ==============================================================================

# Supabase Project URL
# Example: https://abcdefghijklmnopqrst.supabase.co
SUPABASE_URL=your_supabase_url_here

# Supabase Anon Key (PUBLIC - safe to expose in frontend)
# This key is used for client-side authentication
SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Supabase Service Role Key (SECRET - NEVER expose in frontend!)
# Only use this for server-side operations and Edge Functions
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# ==============================================================================
# ANTHROPIC API KEY
# ==============================================================================
# Your Claude API key (server-side fallback for non-BYOK users)
# Get from: https://console.anthropic.com/
CLAUDE_API_KEY=sk-ant-your-api-key-here

# ==============================================================================
# STRIPE (Optional - for billing)
# ==============================================================================
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_key_here
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Stripe Price IDs (create these in Stripe Dashboard)
STRIPE_PRO_PRICE_ID=price_your_pro_price_id_here
STRIPE_ENTERPRISE_PRICE_ID=price_your_enterprise_price_id_here

# ==============================================================================
# PRICING CONFIGURATION
# ==============================================================================
# Markup percentage for non-BYOK users (15% = 0.15)
MARKUP_PERCENTAGE=0.15

# Token limits per tier
FREE_TIER_TOKENS_PER_MONTH=10000
PRO_TIER_INCLUDED_TOKENS=50000

# ==============================================================================
# RATE LIMITING (Optional)
# ==============================================================================
# Redis URL for rate limiting (optional)
# If not set, rate limiting will use in-memory storage
REDIS_URL=redis://localhost:6379/0

# Rate limit settings
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=50
RATE_LIMIT_PER_DAY=500

# ==============================================================================
# APPLICATION SETTINGS
# ==============================================================================
# Environment: development, staging, production
NODE_ENV=development

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=http://localhost:5022,http://127.0.0.1:5022

# ==============================================================================
# NOTES
# ==============================================================================
# 1. NEVER commit this file with real values to git!
# 2. Add .env.supabase to your .gitignore
# 3. For production, use environment variables in your hosting platform
# 4. The SUPABASE_ANON_KEY is public, but SUPABASE_SERVICE_ROLE_KEY must stay secret
# 5. Update static/js/supabaseClient.js with your SUPABASE_URL and SUPABASE_ANON_KEY
# ==============================================================================
